# Vercel Python serverless functions under /api/judge/*
# Kept minimal so cold starts stay under ~1s.
# (Local dev / FastAPI / Anthropic stay in requirements-judge.txt.)
httpx>=0.27,<1.0
pydantic>=2.6,<3.0
supabase>=2.10.0,<3.0
sentry-sdk>=2.0,<3.0
